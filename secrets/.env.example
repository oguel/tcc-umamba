# Modelo de variáveis de ambiente consumidas pelo notebook 00 (Kaggle Secrets / env vars).
# ATENÇÃO: este arquivo é VERSIONADO — NUNCA preencha com valores reais aqui.
# Preencha apenas nos Secrets do Kaggle ou em variáveis de ambiente do runtime.
GDRIVE_TOKEN=
GDRIVE_TOKEN_FILE=
GEE_CREDENTIALS=