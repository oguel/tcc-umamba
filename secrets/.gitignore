# Segredos reais — NUNCA versionar.
# Mantenha aqui APENAS arquivos de tokens locais (git-ignored).
# O que é versionado em secrets/ são só os templates: README.md e .env.example.
drive_token.json
earthengine_credentials.json